worker_processes 1;

events {
    worker_connections 1024;
}

http {
    # Upstream definitions
    upstream blue_pool {
        server blue-app:3000 max_fails=3 fail_timeout=30s;
    }
    
    upstream green_pool {
        server green-app:3000 max_fails=3 fail_timeout=30s;
    }

    # Main server block
    server {
        listen 80;
        
        # Health check for nginx itself
        location /nginx-health {
            access_log off;
            return 200 "nginx healthy\n";
            add_header Content-Type text/plain;
        }

        # Main application routes
        location / {
            # Primary upstream (blue)
            proxy_pass http://blue_pool;
            
            # Failover configuration
            proxy_next_upstream error timeout invalid_header http_500 http_502 http_503 http_504;
            proxy_next_upstream_tries 2;
            proxy_next_upstream_timeout 10s;
            
            # Backup directive for immediate failover
            error_page 502 503 504 = @green_fallback;
            
            # Proxy headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Timeout settings
            proxy_connect_timeout 5s;
            proxy_send_timeout 10s;
            proxy_read_timeout 10s;
            
            # Disable buffering for real-time responses
            proxy_buffering off;
        }
        
        # Fallback location for green pool
        location @green_fallback {
            proxy_pass http://green_pool;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            proxy_connect_timeout 5s;
            proxy_send_timeout 10s;
            proxy_read_timeout 10s;
            proxy_buffering off;
        }
    }
}
